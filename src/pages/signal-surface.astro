---
// New-version preview page — the "Signal & Surface" React design, given
// verbatim by Sahil, hydrated as a single Astro island (client:load).
// Deliberately NOT wired in as the site's actual Home yet: this lets it be
// reviewed side by side with the current index.astro before either
// replacing it or being discarded. See ADR-005 for the full context.
import Base from "../layouts/Base.astro";
import SignalSurfaceHome from "../components/react/SignalSurfaceHome.tsx";
---

<Base
  title="Sahil Gaji — Signal & Surface (preview)"
  description="Preview build: a new Home design for review, not yet live at the site root."
>
  <SignalSurfaceHome client:load />
</Base>
